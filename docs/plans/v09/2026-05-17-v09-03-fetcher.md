# v09-03: Polite Fetcher and Robots Snapshot Handling

## Spec

- Master plan: `docs/plans/v09/README.md`
- Implementation source plan: `docs/plans/2026-05-17-v09-implementation-guide-ff14-crawler.md` (Task 03)
- Crawler spec: `docs/specs/0011-v09-guide-ff14-official-db-crawler.md`

## Status

Completed 2026-05-17

## Goal

guide.ff14.co.kr만 허용하는 GET-only fetcher abstraction을 추가하고 robots snapshot fetch를 구조화한다.

## Scope

- `src/guide_ff14/fetcher.py`
- fake-client tests

Out of scope:

- live network tests
- raw snapshot writing
- crawler CLI

## Red Test

- File: `tests/test_guide_ff14_fetcher.py`
- Expected red reason: `src.guide_ff14.fetcher` module missing.

Contracts fixed by the tests:

- disallowed hosts are rejected before HTTP
- `.get()` is used, not `.head()`
- successful HTML response returns body/hash/encoding
- GET exceptions return structured errors
- robots snapshot fetch uses GET
- delay/sleep is injectable

## Checklist

- [x] fake session tests 작성
- [x] red 상태 확인
- [x] `FetchResult` 추가
- [x] `GuideFetcher` 추가
- [x] standard-library default HTTP session 추가
- [x] docs/handoff 갱신

## Verification

```bash
python -m unittest tests.test_guide_ff14_fetcher tests.test_guide_ff14_category_map tests.test_guide_ff14_storage -v
python -m py_compile src/guide_ff14/fetcher.py src/guide_ff14/__init__.py
git diff --check
python scripts/check_docs_freshness.py --all
```

## Implementation Notes

- Commit: `e47dfab feat: add guide fetcher guardrails`

## Agent Prompt

```text
v09 Task 03을 수행한다. fake HTTP client red tests를 먼저 작성하고 safe GET-only fetcher만 구현한다.
```
