# v09-07: Domain Graph Item Extension

## Spec

- Master plan: `docs/plans/v09/README.md`
- Implementation source plan: `docs/plans/2026-05-17-v09-implementation-guide-ff14-crawler.md` (Task 07)
- Crawler spec: `docs/specs/0011-v09-guide-ff14-official-db-crawler.md`

## Status

Pending

## Goal

existing domain graph generation/report path에 item records를 `Item`, `ItemCategory`, `EquipmentJob`, `ItemSource` nodes/edges로 반영한다.

## Scope

- existing graph rebuild/report code updates
- `tests/test_guide_ff14_item_graph.py`

Out of scope:

- external graph DB
- quest/recipe/gathering graph logic
- live crawl dependency in tests

## Red Test

- File: `tests/test_guide_ff14_item_graph.py`
- Expected red reason: item graph emission not implemented.

Contracts to fix:

- one `guide_items` row creates one `Item` node
- category/job/source/level/provenance edges are emitted when data exists
- graph report includes `Item` count
- rerun is idempotent

## Checklist

- [ ] temp DB/graph red tests 작성
- [ ] red 상태 확인
- [ ] item node/edge emission 구현
- [ ] graph report count 반영
- [ ] existing graph regressions 실행
- [ ] docs/handoff 갱신

## Verification

```bash
python -m unittest tests.test_guide_ff14_item_graph -v
python -m unittest tests.test_domain_graph_rebuild tests.test_graph_report tests.test_v08_e2e -v
git diff --check
python scripts/check_docs_freshness.py --all
```

## Agent Prompt

```text
v09 Task 07을 수행한다. temp DB/graph red tests를 먼저 작성하고 existing graph conventions로 item extension만 구현한다.
```
