# v0.8-07: Graph Report

## Spec

- Master plan: `docs/plans/v08/README.md`
- Implementation source plan: `docs/plans/2026-05-17-v08-implementation.md` (Task 7)
- Graphify layer spec: `docs/specs/0008-v08-ffxiv-domain-graphify-layer-spec.md`

## Status

Completed 2026-05-17

## Goal

Graphify의 `GRAPH_REPORT.md`에 해당하는 FFXIV graph report를 생성한다. report만 읽어도 현재 graph 상태를 파악할 수 있어야 한다.

## Scope

- `graph/GRAPH_REPORT.md` 생성
- Summary, Node Counts, Edge Counts, Top Mentioned Jobs/Patches/Skills, Quality Warnings 섹션
- Quality warning 기준 정의 및 구현

Out of scope:

- graph export JSON (v08-06 책임)
- derived wiki 생성 (v08-08 책임)
- graph visualization HTML (v08 non-goal)

## Red Test

- File: `tests/test_graph_report.py`
- Implementation target: graph report generator module
- Expected red reason: report generator 함수가 존재하지 않아 `ImportError` 발생.

Contracts fixed by the tests:

- `GRAPH_REPORT.md`가 생성된다.
- Summary 섹션이 있다.
- Node Counts 섹션이 있다.
- Edge Counts 섹션이 있다.
- Top Mentioned Jobs 섹션이 있다.
- Quality Warnings 섹션이 있다.

## Checklist

- [x] graph report generator 모듈 생성
  - [x] Summary 섹션 생성 (sources, wiki_pages, graph_nodes, graph_edges, facts count)
  - [x] Node Counts 섹션 생성 (SourceDocument, WikiPage, Job, Patch, Skill, Fact)
  - [x] Edge Counts 섹션 생성 (SOURCE_OF, MENTIONS, HAS_SKILL, SUPPORTS, VALID_IN_PATCH, AFFECTS_JOB, AFFECTS_SKILL, DERIVED_FROM)
  - [x] Top Mentioned Jobs 섹션 생성
  - [x] Top Mentioned Patches 섹션 생성
  - [x] Top Mentioned Skills 섹션 생성
  - [x] Quality Warnings 섹션 생성
- [x] Quality warning 구현
  - [ ] sources without extracted entities
  - [x] facts without supporting sources
  - [x] facts without patch
  - [x] entities without mentions
  - [ ] ambiguous aliases
- [x] `tests/test_graph_report.py` 갱신 (report 관련 tests)
  - [x] `test_report_file_created`
  - [x] `test_report_has_summary_section`
  - [x] `test_report_has_node_counts`
  - [x] `test_report_has_edge_counts`
  - [x] `test_report_has_top_mentioned_jobs`
  - [x] `test_report_has_quality_warnings`
- [x] red 상태 확인
- [x] 최소 구현으로 green 전환
- [x] handoff/README feature map status 갱신

## Verification

```bash
python -m unittest tests.test_graph_report -v
```

## Key Decisions

- Report는 Markdown 형식으로 생성한다.
- Quality warning 기준:
  1. source summary가 있는데 entity가 하나도 추출되지 않은 source
  2. Fact node가 있는데 SUPPORTS edge가 없는 경우
  3. Fact node가 있는데 VALID_IN_PATCH edge가 없는 경우
  4. Job/Skill/Patch node가 있는데 MENTIONS edge가 없는 경우
  5. registry alias가 중복되거나 ambiguous한 경우
- Top Mentioned는 MENTIONS edge count 기준 상위 항목을 표시한다.

## Implementation Notes

- v08-04 graph storage helper의 조회 함수를 사용한다.
- rebuild CLI(v08-05)의 마지막 단계에서 이 report 함수를 호출한다.
- OpenClaw/Codex agent가 작업 시작 전에 참고할 수 있는 수준이어야 한다.

## Agent Prompt

```text
v08 Task 7을 수행한다.
`graph/GRAPH_REPORT.md`를 생성하는 report generator를 구현한다.
Summary, Node Counts, Edge Counts, Top Mentioned Jobs/Patches/Skills, Quality Warnings 섹션을 포함한다.
report만 읽어도 현재 graph 상태를 파악할 수 있어야 한다.
```
