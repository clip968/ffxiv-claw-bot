# v0.8-10: End-to-End Smoke Test

## Spec

- Master plan: `docs/plans/v08/README.md`
- Implementation source plan: `docs/plans/2026-05-17-v08-implementation.md` (Task 10)
- Graphify layer spec: `docs/specs/0008-v08-ffxiv-domain-graphify-layer-spec.md`

## Status

Pending

## Goal

v08 전체 파이프라인이 최소 fixture에서 end-to-end로 동작하는지 검증한다. fixture source summary 하나로 domain graph rebuild → derived wiki generation → graph report generation → hybrid retrieval까지 검증한다.

## Scope

- fixture source summary 준비
- domain graph rebuild 실행
- derived wiki 생성 확인
- graph report 생성 확인
- ask query 실행 및 graph + FTS context 확인
- 실제 DB를 오염시키지 않도록 tmp DB 사용

Out of scope:

- production data 테스트
- performance benchmarking
- Discord integration 테스트

## Red Test

- File: `tests/test_v08_e2e.py`
- Implementation target: E2E integration
- Expected red reason: v08 pipeline 모듈이 연결되지 않아 import 또는 assertion 실패.

Contracts fixed by the tests:

- fixture source summary에서 entity extraction이 동작한다.
- domain graph rebuild 후 Job, Patch, Skill, Fact node가 생성된다.
- derived wiki 파일이 생성된다.
- graph report 파일이 생성된다.
- hybrid retrieval이 entity match + graph neighborhood를 반환한다.

## Checklist

- [ ] `tests/test_v08_e2e.py` 생성
- [ ] Fixture 준비
  - [ ] fixture source summary 텍스트 (`Patch 7.5 includes adjustments to Gunbreaker. No Mercy duration was changed.`)
  - [ ] tmp DB 생성
  - [ ] tmp wiki 디렉터리
  - [ ] tmp graph 디렉터리
- [ ] E2E 시나리오 테스트
  - [ ] `test_e2e_entity_extraction` — fixture에서 job:gunbreaker, patch:7_5, skill:no_mercy 추출
  - [ ] `test_e2e_domain_graph_rebuild` — rebuild 후 node/edge 존재 확인
  - [ ] `test_e2e_derived_wiki_generation` — wiki/jobs/gunbreaker.md, wiki/patches/7_5.md, wiki/skills/no_mercy.md 생성 확인
  - [ ] `test_e2e_graph_report` — graph/GRAPH_REPORT.md 생성 확인
  - [ ] `test_e2e_hybrid_retrieval` — `건브 7.5 변경점 알려줘` 질문에서 entity match + context 반환
- [ ] Cleanup
  - [ ] tmp DB/파일 정리 (tearDown)
- [ ] red 상태 확인
- [ ] 최소 구현으로 green 전환
- [ ] handoff/README feature map status 갱신

## Verification

```bash
python -m unittest tests.test_v08_e2e -v
```

Full suite:

```bash
python -m unittest discover -s tests -p "test_*.py"
```

## Key Decisions

- 실제 DB를 오염시키지 않도록 tmp DB 또는 in-memory SQLite를 사용한다.
- 테스트 질문:
  - `건브 7.5 변경점 알려줘`
  - `No Mercy 관련 변경 있어?`
  - `7.5에서 언급된 직업 보여줘`
- 단일 test command(`python -m unittest tests.test_v08_e2e -v`)로 전체 E2E fixture가 통과해야 한다.

## Implementation Notes

- v08-01~09 모든 모듈에 의존한다.
- fixture source summary는 테스트 내부에 inline으로 정의하거나 `tests/fixtures/` 하위에 둔다.
- E2E 테스트는 v08의 최종 acceptance criteria를 검증하는 역할이다.

## Agent Prompt

```text
v08 Task 10을 수행한다.
v08 end-to-end smoke test를 작성한다.
fixture source summary 하나로 domain graph rebuild, derived wiki generation, graph report generation, hybrid retrieval까지 검증한다.
실제 DB를 오염시키지 않도록 tmp DB 또는 transaction rollback을 사용한다.
```
