# v0.8.5-07: Add v08.5 Tests

## Spec

- Master plan: `docs/plans/v08_5/README.md`
- Implementation source plan: `docs/plans/2026-05-17-v08_5_implementation.md` (Task 7)
- Activation spec: `docs/specs/0009-v08_5_managed_wiki_kb_activation_spec.md`

## Status

Pending

## Goal

v08.5에서 확보한 graph population, derived wiki activation, FTS visibility, answer quality를 regression으로 고정한다.

## Scope

- v08.5 acceptance criteria를 명시적으로 검증하는 테스트 작성
- fixture 기반 격리된 테스트 (실제 repo 데이터에 의존하지 않음)
- 기존 v06/v07/v08 테스트 통과 확인

Out of scope:

- production data 테스트
- performance benchmarking

## Red Test

이 task 자체가 red test 작성 및 green 전환을 위한 task다. 각 테스트 파일은 v08_5-02, 04, 05, 06에서 이미 생성되었을 수 있으며, 이 task에서 통합 검증한다.

## Checklist

- [ ] 테스트 파일 확인/정리
  - [ ] `tests/test_v08_5_real_graph_population.py` (v08_5-02에서 생성)
  - [ ] `tests/test_v08_5_real_derived_wiki.py` (v08_5-04에서 생성)
  - [ ] `tests/test_v08_5_fts_visibility.py` (v08_5-05에서 생성)
  - [ ] `tests/test_v08_5_answer_quality.py` (v08_5-06에서 생성)
- [ ] 공통 fixture 확인
  - [ ] registry fixture: Gunbreaker/GNB/건브/건브레이커, No Mercy/노머시, Patch 7.5/7.5/패치 7.5
  - [ ] source summary fixture: `Patch 7.5 includes adjustments to Gunbreaker. No Mercy duration was changed.`
  - [ ] `tempfile.TemporaryDirectory()` 사용
  - [ ] tmp DB 사용
- [ ] Graph population assertions
  - [ ] `job:gunbreaker` node exists
  - [ ] `patch:7_5` node exists
  - [ ] `skill:no_mercy` node exists
  - [ ] `fact:*` node exists
  - [ ] required edge types exist
- [ ] Derived wiki assertions
  - [ ] `wiki/jobs/gunbreaker.md` exists
  - [ ] `wiki/patches/7_5.md` exists
  - [ ] `wiki/skills/no_mercy.md` exists
  - [ ] related source text exists
  - [ ] `wiki/index.md` links generated pages
- [ ] FTS visibility assertions
  - [ ] `wiki_pages` has generated wiki pages
  - [ ] ask/retrieval can include derived wiki context
  - [ ] source summary fallback still works
- [ ] Answer quality assertions
  - [ ] answer includes `요약`
  - [ ] answer includes `근거`
  - [ ] answer includes relevant entity text
  - [ ] answer does not equal raw source body
  - [ ] answer includes uncertainty when contexts are sparse
- [ ] 모든 v08.5 테스트 통과
- [ ] 기존 v06/v07/v08 테스트 통과
- [ ] handoff/README feature map status 갱신

## Verification

개별 실행:

```bash
python -m unittest tests.test_v08_5_real_graph_population -v
python -m unittest tests.test_v08_5_real_derived_wiki -v
python -m unittest tests.test_v08_5_fts_visibility -v
python -m unittest tests.test_v08_5_answer_quality -v
```

Full suite:

```bash
python -m unittest discover -s tests -p "test_*.py"
```

## Key Decisions

- 테스트는 실제 repo의 현재 source summary 개수에 의존하지 않는다.
- `tempfile.TemporaryDirectory()`와 tmp DB를 사용하여 격리한다.
- v08 E2E test와 중복되더라도 v08.5 acceptance criteria를 명시적으로 검증한다.

## Implementation Notes

- 파일 수가 과하면 `tests/test_v08_5_activation.py`와 `tests/test_v08_5_answer_quality.py`로 묶어도 된다.
- 각 task에서 이미 red test를 작성했으면 이 task에서는 통합 검증만 수행한다.
- 테스트가 아직 red이면 해당 task로 돌아가 구현을 완료한다.

## Agent Prompt

```text
v08.5 Task 7을 수행한다.
v08.5의 모든 테스트가 작성되었는지 확인하고, 누락된 테스트를 추가한다.
모든 v08.5 테스트가 통과하는지 확인한다.
기존 v06/v07/v08 테스트가 깨지지 않는지 확인한다.
```
