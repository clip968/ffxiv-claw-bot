# v05.1-04: fetch_url Routing

## Spec

- Master plan: `docs/plans/v05.1/README.md`
- Hardening spec: `docs/specs/0004a-v05.1-source-processing-hardening.md`
- Requirements: REQ-0004A-012, REQ-0004A-014
- Acceptance criteria: AC-0004A-005 through AC-0004A-009

## Status

Completed 2026-05-16

## Goal

`tools/fetch_url.py`가 Lodestone HTML URL을 generic HTML extractor보다 Lodestone extractor로 먼저 라우팅하도록 만든다.

## Scope

- Lodestone URL + `text/html` response를 `extract_lodestone_article()`로 라우팅
- non-Lodestone HTML은 기존 generic extraction 유지
- `text/plain`, `application/json`, `+json` 동작 유지
- unsupported content type/error behavior 회귀 방지
- `fetch_single_url()` result에 `extractor` metadata 추가

Out of scope:

- network stack 교체
- recursive crawling
- sitemap parsing
- auth-required URL 처리
- `process_source.py` action metadata, owned by v05.1-05

## Red Tests

- File: `tests/test_v05_fetch_url.py` or `tests/test_v05_1_fetch_url_routing.py`
- Implementation target: `tools/fetch_url.py`
- Current red reason: `fetch_single_url()` does not route Lodestone HTML through a domain-specific extractor and does not return extractor metadata.

Contracts fixed by the tests:

- Lodestone HTML URL returns `extractor=lodestone`.
- Lodestone HTML uses the article body from `.news__detail__wrapper`.
- Non-Lodestone HTML returns `extractor=generic_html`.
- Plain text returns `extractor=text` or the stable convention chosen in implementation.
- JSON returns `extractor=json` or the stable convention chosen in implementation.
- Unsupported content type remains an error.

## Checklist

- [x] Add red test for Lodestone URL routing
- [x] Add or update test for generic non-Lodestone HTML routing
- [x] Add or update test for text/plain extractor metadata
- [x] Add or update test for JSON extractor metadata
- [x] Confirm tests fail for expected reason before implementation
- [x] Import Lodestone extractor in `tools/fetch_url.py`
- [x] Route `text/html` + Lodestone URL to `extract_lodestone_article()`
- [x] Preserve existing generic HTML extraction for other URLs
- [x] Add `extractor` to `fetch_single_url()` result
- [x] Preserve existing `url`, `content_type`, `title`, and `body` result fields
- [x] Keep existing fetch errors and unsupported content-type behavior compatible
- [x] Run focused and existing v05 tests

## Verification

```bash
python -m unittest tests.test_v05_fetch_url -v
python -m unittest tests.test_v05_1_lodestone_extractor -v
```

If a new routing-specific test module is created:

```bash
python -m unittest tests.test_v05_1_fetch_url_routing -v
```

## Key Decisions

- Lodestone routing belongs in `fetch_url.py`, not in `process_source.py`.
- `process_source.py` should receive normalized fetch metadata from `fetch_single_url()`.
- Generic HTML extraction must remain available for non-Lodestone HTML pages.

## Implementation Notes

- Do not perform real network requests in tests; use the existing mocked fetch pattern from v05 tests.
- Do not add crawler behavior.

## Verification Results

- Expected red confirmed before implementation:
  - `python -m unittest tests.test_v05_fetch_url -v`: failed because `extractor` was missing and Lodestone HTML still used the generic HTML title path.
- Green verification after implementation:
  - `python -m unittest tests.test_v05_fetch_url -v`: OK, 6 tests.
  - `python -m unittest tests.test_v05_1_lodestone_extractor -v`: OK, 5 tests.
