# v05.1-08: Final Verification and Handoff

## Spec

- Master plan: `docs/plans/v05.1/README.md`
- Hardening spec: `docs/specs/0004a-v05.1-source-processing-hardening.md`
- Parent pipeline spec: `docs/specs/0004-v05-source-processing-pipeline.md`
- Workflow: `docs/WORKFLOW.md`
- Handoff: `docs/handoff/CURRENT_HANDOFF.md`

## Status

Pending

## Goal

v05.1 hardening이 완료됐는지 focused tests, full unittest, docs freshness, whitespace check, finish gate로 검증하고 handoff에 다음 세션이 필요한 정보를 남긴다.

## Scope

- v05.1 focused tests 실행
- existing v0.5 focused tests 실행
- full unittest suite 실행
- docs freshness 실행
- whitespace check 실행
- finish gate 실행
- `docs/handoff/CURRENT_HANDOFF.md` 갱신
- `docs/plans/v05.1/README.md` status 갱신
- completion report 작성

Out of scope:

- 새로운 behavior 구현
- 실패 테스트 숨기기
- Notion API real apply
- commit 또는 push, unless maintainer explicitly requests it

## Red Test

No new red test required.

This is the final verification and handoff task. Any failing test found here must be reported directly and fixed in the owning earlier task.

## Checklist

- [ ] Confirm v05.1-02 through v05.1-07 statuses
- [ ] Run Lodestone extractor focused tests
- [ ] Run fetch URL focused tests
- [ ] Run process source focused tests
- [ ] Run full unittest suite
- [ ] Run docs freshness
- [ ] Run whitespace check
- [ ] Run final finish gate
- [ ] Update `docs/handoff/CURRENT_HANDOFF.md`
- [ ] Update `docs/plans/v05.1/README.md`
- [ ] Record verification results in each completed task plan
- [ ] Report remaining limitations honestly
- [ ] Do not commit or push unless explicitly requested

## Verification

```bash
python -m unittest tests.test_v05_1_lodestone_extractor -v
python -m unittest tests.test_v05_fetch_url -v
python -m unittest tests.test_v05_process_source -v
python -m unittest discover -s tests -p "test_*.py"
python scripts/check_docs_freshness.py --all
git diff --check
python scripts/finish_task.py
```

## Completion Report Format

```text
1. 발견한 문제

2. 수정한 파일

3. 추가한 테스트

4. 통과한 검증 명령

5. 남은 제한 사항
```

## Key Decisions

- Completion claims require command output, not only implementation summary.
- `finish_task.py` runs after handoff is updated because it is the final gate.
- Real Notion apply is not required for v05.1 completion.

## Implementation Notes

- If `python scripts/finish_task.py` fails because Notion dry-run integration is unavailable, report the exact failure instead of claiming completion.
- Keep v0.6 automation loop out of this completion task.

## Verification Results

- Pending.
